# 🚀 دليل التثبيت للزبون - تثبيت لأول مرة

## ⚠️ المتطلبات قبل البدء

يجب أن يكون مثبتاً على الجهاز:
- ✅ **XAMPP** أو **PHP** (الإصدار 8.1 أو أحدث)
- ✅ **Composer** - من: https://getcomposer.org/
- ✅ **MySQL** أو **MariaDB** (يأتي مع XAMPP)

❌ **لا تحتاج**:
- Node.js
- npm
- Git

---

## 📋 خطوات التثبيت (بالتفصيل)

### 1️⃣ فك ضغط الملف

افتح ملف `car-dealer-ship-v1.0-client.zip` واستخرج محتوياته إلى المكان المطلوب، مثلاً:
```
C:\xampp\htdocs\car-dealer-ship\
```

---

### 2️⃣ تشغيل ملف التثبيت

1. افتح مجلد المشروع
2. ابحث عن ملف: **`INSTALL.bat`**
3. اضغط عليه **دبل كليك**

سيقوم الملف بـ:
- ✅ فحص وجود PHP و Composer
- ✅ تثبيت مكتبات PHP تلقائياً
- ✅ إنشاء ملف `.env`
- ✅ إنشاء المجلدات المطلوبة

**انتظر حتى اكتمال التثبيت (2-3 دقائق)**

---

### 3️⃣ إعداد قاعدة البيانات

#### أ. إنشاء قاعدة بيانات:

1. افتح **phpMyAdmin**: http://localhost/phpmyadmin
2. اضغط على **"New"** أو **"جديد"**
3. أدخل اسم قاعدة البيانات، مثلاً: `car_dealership`
4. اضغط **"Create"** أو **"إنشاء"**

#### ب. تعديل ملف `.env`:

1. افتح المجلد: `Backend\`
2. افتح ملف: `.env` بأي محرر نصوص
3. ابحث عن السطور التالية وعدّلها:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=car_dealership        ← ضع الاسم الذي أنشأته
DB_USERNAME=root                  ← عادة root
DB_PASSWORD=                      ← كلمة المرور (فارغة في XAMPP)
```

4. احفظ الملف

---

### 4️⃣ تشغيل Migrations

افتح **Command Prompt** أو **PowerShell** في مجلد المشروع:

```bash
cd Backend
php artisan migrate
```

إذا سألك: `Do you want to run migrations? (yes/no)`
اكتب: `yes`

---

### 5️⃣ (اختياري) إضافة بيانات تجريبية

إذا أردت بيانات تجريبية للاختبار:

```bash
php artisan db:seed
```

---

### 6️⃣ تشغيل الخادم

في مجلد `Backend`:

```bash
php artisan serve
```

ستظهر رسالة مثل:
```
Server started on http://127.0.0.1:8000
```

**لا تغلق هذه النافذة!** اتركها مفتوحة أثناء استخدام النظام.

---

### 7️⃣ فتح النظام في المتصفح

افتح المتصفح واذهب إلى:
```
http://localhost:8000
```

أو

```
http://127.0.0.1:8000
```

**✅ مبروك! النظام يعمل الآن!**

---

## 🔄 التحديثات المستقبلية

عندما يتوفر تحديث:

1. **تأكد من وجود اتصال إنترنت**
2. **اضغط دبل كليك على:** `update.bat`
3. **انتظر 2-5 دقائق**
4. **✅ تم التحديث!**

النظام سيقوم تلقائياً بـ:
- نسخ احتياطي لقاعدة البيانات
- تحميل آخر التحديثات
- تحديث المكتبات
- تحديث قاعدة البيانات

---

## 🆘 حل المشاكل الشائعة

### المشكلة: "composer: command not found"
**الحل:** تثبيت Composer من: https://getcomposer.org/

### المشكلة: "Could not find driver"
**الحل:** تفعيل PHP extensions في `php.ini`:
```ini
extension=pdo_mysql
extension=mysqli
```

### المشكلة: "SQLSTATE[HY000] [1045] Access denied"
**الحل:** تحقق من بيانات قاعدة البيانات في `Backend\.env`

### المشكلة: صفحة بيضاء أو خطأ 500
**الحل:** 
```bash
cd Backend
php artisan cache:clear
php artisan config:clear
```

---

## 📞 للدعم الفني

للمزيد من التفاصيل، اقرأ:
- **CLIENT_GUIDE.md** - دليل شامل
- **QUICK_START.md** - مرجع سريع

---

## 📊 ملخص الأوامر

```bash
# التثبيت الأولي
INSTALL.bat                    # تشغيل مرة واحدة فقط

# بعد تعديل .env
cd Backend
php artisan migrate            # إنشاء الجداول
php artisan db:seed            # (اختياري) بيانات تجريبية

# تشغيل الخادم
php artisan serve              # في كل مرة تريد استخدام النظام

# التحديث
update.bat                     # عند توفر تحديث جديد
```

---

## ✅ Checklist

- [ ] XAMPP/PHP مثبت
- [ ] Composer مثبت
- [ ] MySQL يعمل
- [ ] فك ضغط المشروع
- [ ] تشغيل INSTALL.bat
- [ ] إنشاء قاعدة البيانات
- [ ] تعديل Backend\.env
- [ ] php artisan migrate
- [ ] php artisan serve
- [ ] فتح المتصفح
- [ ] ✅ النظام يعمل!

---

**🎉 مبروك! أنت الآن جاهز لاستخدام النظام!**

_للتحديثات المستقبلية: فقط اضغط على `update.bat`_
