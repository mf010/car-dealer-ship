# 📘 دليل التحديث للمطور
# Developer's Update Guide

## نظام التحديث بدون npm على جهاز العميل
هذا المشروع يستخدم نظام تحديث مبتكر يسمح للعميل بتحديث النظام بضغطة زر واحدة **بدون الحاجة لـ Node.js أو npm**.

---

## 🎯 المفهوم الأساسي

**المشكلة:** 
- العميل لا يمتلك Node.js أو npm
- العميل يعمل بدون إنترنت أغلب الوقت
- العميل لا يريد تنفيذ أوامر معقدة

**الحل:**
- المطور يبني الـ React على جهازه
- يتم رفع مجلد `dist` إلى GitHub
- العميل يستخدم `update.bat` للتحديث
- Git Portable مضمّن في المشروع

---

## 📋 خطوات العمل للمطور

### 1️⃣ التطوير المحلي (على جهازك)

```bash
# الانتقال لمجلد الواجهة
cd frontend

# تطوير عادي
npm run dev

# اختبار التعديلات
```

---

### 2️⃣ بناء الواجهة (React Build)

عندما تنتهي من التطوير وتريد رفع تحديث للعميل:

```bash
# التأكد من كون المجلد الحالي هو frontend
cd frontend

# بناء الواجهة للإنتاج
npm run build
```

**النتيجة:** سيتم إنشاء مجلد `frontend/dist` يحتوي على الملفات الجاهزة.

---

### 3️⃣ التحقق من .gitignore

تأكد من أن ملف `frontend/.gitignore` **لا يمنع رفع** مجلد `dist`:

```gitignore
# ✅ الصحيح - السطر معلق
# dist

# ❌ الخطأ - السطر نشط
dist
```

> **ملاحظة:** تم تعديل الملف تلقائياً في هذا المشروع لتضمين dist.

---

### 4️⃣ رفع التحديث إلى GitHub

```bash
# العودة لمجلد المشروع الرئيسي
cd ..

# إضافة جميع الملفات بما فيها dist
git add .

# كتابة رسالة الـ commit
git commit -m "Update: تحسينات على الواجهة + dist build"

# رفع إلى GitHub
git push origin main
```

**⚠️ مهم جداً:**
- تأكد من إضافة مجلد `dist` في الـ commit
- استخدم رسائل commit واضحة بالعربية والإنجليزية

---

### 5️⃣ تحديث رقم الإصدار (اختياري لكن موصى به)

بعد كل push، سيتم تحديث `version.json` تلقائياً عند تشغيل `update.bat` على جهاز العميل.

---

## 🔄 سير العمل الكامل

```
1. تطوير محلي         →   npm run dev
                         ↓
2. بناء الواجهة        →   npm run build
                         ↓
3. التحقق من dist      →   التأكد من وجود frontend/dist
                         ↓
4. Git Add & Commit    →   git add . && git commit
                         ↓
5. Push إلى GitHub     →   git push origin main
                         ↓
6. العميل يُحدّث       →   يضغط على update.bat
```

---

## 📦 ماذا يحدث على جهاز العميل؟

عندما يضغط العميل على `update.bat`:

1. ✅ نسخ احتياطي تلقائي لقاعدة البيانات
2. ✅ سحب آخر تحديثات من GitHub (بما فيها dist)
3. ✅ تحديث مكتبات PHP عبر Composer
4. ✅ تشغيل Database Migrations
5. ✅ تحديث رقم الإصدار
6. ✅ حفظ log كامل للعملية

---

## 🚀 نصائح مهمة للمطور

### ✅ افعل:
- دائماً قم بعمل `npm run build` قبل الـ push
- تأكد من اختبار البناء محلياً قبل الرفع
- استخدم رسائل commit واضحة
- احتفظ بنسخ احتياطية من التحديثات الكبيرة

### ❌ لا تفعل:
- لا ترفع `node_modules` (مستثنى افتراضياً)
- لا تضع `dist` في `.gitignore`
- لا تنسى بناء الواجهة قبل Push
- لا تعدل ملف `update.bat` إلا للضرورة

---

## 🔧 تعديلات Backend

إذا أضفت Migrations جديدة:

```bash
# في مجلد Backend
php artisan make:migration create_something_table

# تطوير واختبار محلي
php artisan migrate

# ثم push - سيتم تشغيل migrate تلقائياً على جهاز العميل
```

إذا أضفت مكتبات Composer جديدة:

```bash
composer require package/name

# التحديث سيشمل هذا تلقائياً عبر composer install
```

---

## 📊 مراقبة الإصدارات

بعد كل تحديث على جهاز العميل، سيتم إنشاء:

```
📁 logs/
   └── update_20250122_143052.log    ← سجل كامل للتحديث

📁 backups/
   └── db_backup_20250122_143052.sql ← نسخة احتياطية تلقائية

📄 version.json                       ← معلومات الإصدار الحالي
```

---

## 🆘 استكشاف الأخطاء

### المشكلة: dist غير موجود بعد Pull
**الحل:** تأكد من أنك قمت بـ `npm run build` قبل Push

### المشكلة: التحديث فشل على جهاز العميل
**الحل:** 
1. اطلب من العميل إرسال ملف اللوج من `logs/`
2. تحقق من رسائل الخطأ
3. يمكن استرجاع النسخة الاحتياطية من `backups/`

### المشكلة: Migrations تفشل
**الحل:**
- تأكد من اختبار الـ migrations محلياً قبل Push
- تحقق من التوافق مع الإصدار القديم من قاعدة البيانات

---

## 🎓 خلاصة سريعة

```bash
# على جهاز المطور
cd frontend && npm run build && cd ..
git add .
git commit -m "Update: وصف التحديث"
git push origin main

# على جهاز العميل
double-click update.bat ← فقط!
```

---

## 📞 للدعم

عند حدوث مشاكل، اطلب من العميل:
- ملف اللوج من `logs/update_*.log`
- محتوى `version.json`
- لقطة شاشة لنافذة update.bat

---

**✨ نصيحة أخيرة:**
احتفظ بنسخة من آخر build ناجح على جهازك كخطة احتياطية!
