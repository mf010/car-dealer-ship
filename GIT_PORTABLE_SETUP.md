# 🛠️ دليل إعداد Git Portable
# Git Portable Setup Guide

## 📥 تحميل Git Portable

### الطريقة الأولى: التحميل المباشر

1. اذهب إلى الموقع الرسمي:
   ```
   https://git-scm.com/download/win
   ```

2. ابحث عن قسم **Portable ("thumbdrive edition")**

3. اختر النسخة المناسبة:
   - **64-bit:** PortableGit-2.x.x-64-bit.7z.exe
   - **32-bit:** PortableGit-2.x.x-32-bit.7z.exe

4. حمّل الملف (حوالي 50 MB)

### الطريقة الثانية: الرابط المباشر

```
https://github.com/git-for-windows/git/releases/latest
```
ابحث عن ملفات تبدأ بـ `PortableGit-`

---

## 📂 التثبيت داخل المشروع

### الخطوة 1: فك الضغط

1. قم بتشغيل الملف الذي حملته (مثلاً: `PortableGit-2.43.0-64-bit.7z.exe`)
2. سيطلب منك اختيار مكان الاستخراج
3. اختر مجلد **مؤقت** (مثلاً: `C:\Temp\git-portable`)

### الخطوة 2: نقل إلى المشروع

1. افتح مجلد المشروع:
   ```
   C:\xampp\htdocs\car-dealer-ship\
   ```

2. انسخ المجلد المستخرج بالكامل وأعد تسميته إلى `git`:
   ```
   C:\xampp\htdocs\car-dealer-ship\git\
   ```

### الخطوة 3: التحقق من البنية

يجب أن تكون البنية كالتالي:

```
car-dealer-ship/
├── update.bat
├── git/                     ← Git Portable هنا
│   ├── bin/
│   │   ├── git.exe         ← مهم
│   │   ├── bash.exe
│   │   └── ...
│   ├── cmd/
│   │   ├── git.exe         ← مهم
│   │   └── ...
│   ├── mingw64/
│   ├── usr/
│   └── ...
├── Backend/
├── frontend/
└── ...
```

---

## ✅ التحقق من التثبيت

### طريقة 1: عبر نافذة الأوامر

افتح PowerShell في مجلد المشروع:

```powershell
# التحقق من وجود git.exe
Test-Path ".\git\bin\git.exe"
# يجب أن تظهر: True

# التحقق من النسخة
.\git\bin\git.exe --version
# يجب أن تظهر: git version 2.x.x
```

### طريقة 2: تشغيل update.bat تجريبياً

1. اضغط دبل كليك على `update.bat`
2. إذا ظهرت رسالة **"تم العثور على Git Portable"** ✅
3. إذا ظهرت رسالة **"Git Portable غير موجود"** ❌

---

## 🔧 إعداد Git للمشروع

### المرة الأولى فقط:

1. افتح PowerShell في مجلد المشروع

2. قم بتهيئة Git:

```powershell
# تعيين المسار
$env:PATH = "$PWD\git\bin;$PWD\git\cmd;$env:PATH"

# التحقق من الإعداد الحالي
git config --global user.name
git config --global user.email

# إذا لم يكن معد، قم بالإعداد:
git config --global user.name "اسمك"
git config --global user.email "email@example.com"

# التحقق من الاتصال بـ GitHub
git remote -v
# يجب أن تظهر روابط المشروع على GitHub
```

3. إذا لم يكن المشروع متصلاً بـ GitHub:

```powershell
# إضافة Remote
git remote add origin https://github.com/username/car-dealer-ship.git

# أو إذا كان موجوداً لكن خاطئ
git remote set-url origin https://github.com/username/car-dealer-ship.git
```

---

## 🌐 إعداد الاتصال بـ GitHub

### الطريقة الأولى: HTTPS (موصى بها للعميل)

1. تأكد من أن الـ remote يستخدم HTTPS:
   ```powershell
   git remote -v
   # يجب أن يظهر: https://github.com/...
   ```

2. عند أول `git pull`، سيطلب منك:
   - Username: اسم المستخدم في GitHub
   - Password: Personal Access Token (ليس كلمة المرور العادية)

3. إنشاء Personal Access Token:
   - اذهب إلى: https://github.com/settings/tokens
   - Generate new token (classic)
   - اختر Scopes: `repo` (كامل)
   - انسخ الـ Token واستخدمه كـ password

### الطريقة الثانية: SSH (للمطورين)

إذا كنت تستخدم SSH Keys:

```powershell
# التحقق من SSH
git remote -v
# يجب أن يظهر: git@github.com:...

# اختبار الاتصال
ssh -T git@github.com
```

---

## 🔐 حفظ بيانات الاعتماد

لتجنب إدخال Username/Password في كل مرة:

```powershell
# تفعيل Credential Manager
git config --global credential.helper wincred

# الآن عند أول pull، سيتم حفظ البيانات تلقائياً
```

---

## 📝 إضافة Git Portable إلى .gitignore

**مهم جداً!** يجب عدم رفع مجلد `git/` إلى GitHub.

افتح ملف `.gitignore` في جذر المشروع وأضف:

```gitignore
# Git Portable - لا نحتاج رفعه
/git/

# اللوجات والنسخ الاحتياطية - خاصة بكل جهاز
/logs/
/backups/
/version.json
```

---

## 📦 حزمة جاهزة للعميل (للمطور)

إذا أردت إرسال نسخة جاهزة للعميل:

### الخطوة 1: تجهيز Git Portable

```powershell
# في مجلد المشروع
# 1. حمّل Git Portable
# 2. ضعه في مجلد git/
# 3. تحقق من التثبيت
```

### الخطوة 2: ضغط المشروع

```
car-dealer-ship/
├── update.bat          ← أساسي
├── git/                ← مضمّن
│   └── bin/
│       └── git.exe
├── Backend/            ← بدون vendor/
├── frontend/           ← مع dist/
└── CLIENT_GUIDE.md     ← دليل العميل
```

### الخطوة 3: إرسال للعميل

- احذف `Backend/vendor/` قبل الضغط (كبير الحجم)
- احذف `node_modules/` (غير مطلوب)
- اضغط المجلد كاملاً إلى `car-dealer-ship.zip`
- أرسله للعميل

### الخطوة 4: تعليمات العميل

عند استلام المشروع:

```
1. فك ضغط car-dealer-ship.zip
2. افتح مجلد Backend
3. شغّل: composer install
4. اضبط ملف .env (قاعدة البيانات)
5. شغّل: php artisan key:generate
6. شغّل: php artisan migrate
7. جاهز! الآن استخدم update.bat للتحديثات المستقبلية
```

---

## 🧪 اختبار النظام

### اختبار 1: Git Pull

```powershell
# في مجلد المشروع
.\git\bin\git.exe pull origin main
```

**النتيجة المتوقعة:**
- إما: `Already up to date` ✅
- أو: تحميل ملفات جديدة ✅
- خطأ في الاتصال ❌ (راجع إعداد GitHub)

### اختبار 2: تشغيل update.bat

```
1. دبل كليك على update.bat
2. تابع الرسائل
3. يجب أن تمر جميع الخطوات بنجاح
```

### اختبار 3: Composer Install

```powershell
cd Backend
composer install
```

يجب أن يكتمل بدون أخطاء.

---

## ❓ الأسئلة الشائعة

### ❓ ما حجم Git Portable؟
حوالي 50-60 MB بعد فك الضغط.

### ❓ هل يمكن استخدام Git المثبت في النظام؟
نعم، لكن النظام مصمم للعمل بدون أي تثبيتات خارجية.

### ❓ هل Git Portable يحتاج صلاحيات Admin؟
لا، يعمل بدون صلاحيات إدارية.

### ❓ هل يمكن تحديث Git Portable؟
نعم، فقط حمّل نسخة أحدث وبدّل المجلد.

### ❓ ماذا لو حذف العميل مجلد git/ بالخطأ؟
فقط أعد وضع نسخة جديدة من Git Portable.

---

## 📊 حجم الملفات المتوقع

```
git/                    ~60 MB
Backend/vendor/         ~30 MB (سيتم تحميله عبر composer)
frontend/dist/          ~2-5 MB
frontend/node_modules/  ~300 MB (غير مطلوب للعميل)
```

**المجموع للعميل:** حوالي 100 MB بدون node_modules

---

## ✅ checklist النهائي

قبل إرسال المشروع للعميل، تأكد من:

- [ ] Git Portable موجود في `git/`
- [ ] `update.bat` يعمل بدون مشاكل
- [ ] `.gitignore` يستثني `git/` و `logs/` و `backups/`
- [ ] `frontend/dist/` موجود ومحدث
- [ ] `CLIENT_GUIDE.md` موجود
- [ ] Remote GitHub معد بشكل صحيح
- [ ] Credentials محفوظة أو معدة

---

**🎉 الآن النظام جاهز للعمل!**

العميل يحتاج فقط لـ:
1. Composer مثبت (لأول مرة فقط)
2. PHP مثبت
3. اتصال إنترنت (عند التحديث فقط)
4. update.bat (للتحديثات)

**لا حاجة لـ Node.js أو npm أو Git مثبت في النظام! 🚀**
