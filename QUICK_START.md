# ⚡ دليل البدء السريع - Quick Start Guide

## 🎯 للمطور (على جهازك)

### الإعداد الأولي (مرة واحدة):

```bash
# 1. تحميل Git Portable
# من: https://git-scm.com/download/win
# ضعه في: car-dealer-ship/git/

# 2. التأكد من .gitignore
# تأكد أن frontend/.gitignore لا يمنع dist
# (تم التعديل تلقائياً)

# 3. أول build و push
cd frontend
npm run build
cd ..

git add .
git commit -m "Setup: نظام التحديث + dist"
git push origin main
```

---

### سير العمل اليومي:

```bash
# 1. التطوير
cd frontend
npm run dev
# ... قم بالتعديلات ...

# 2. البناء
npm run build

# 3. الرفع
cd ..
git add .
git commit -m "Update: وصف التحديل"
git push origin main

# ✅ انتهى! العميل الآن يمكنه التحديث
```

---

## 🎯 للعميل (على جهاز العميل)

### التحديث (في أي وقت):

```
1. دبل كليك على: update.bat
2. انتظر 2-5 دقائق
3. ✅ تم!
```

**أو** من المتصفح:
```
http://localhost:8000/api/system/update
اضغط زر "تحديث النظام"
```

---

## 📋 الملفات المهمة

| الملف | الوصف | للمطور | للعميل |
|------|-------|---------|---------|
| `update.bat` | ملف التحديث | ❌ | ✅ |
| `DEVELOPER_GUIDE.md` | دليل المطور الكامل | ✅ | ❌ |
| `CLIENT_GUIDE.md` | دليل العميل الكامل | ❌ | ✅ |
| `GIT_PORTABLE_SETUP.md` | إعداد Git | ✅ | ❌ |
| `README_UPDATE_SYSTEM.md` | نظرة عامة شاملة | ✅ | ✅ |

---

## 🚨 المشاكل الشائعة

### "Git Portable غير موجود"
```
الحل: ضع Git Portable في مجلد git/
راجع: GIT_PORTABLE_SETUP.md
```

### "dist غير موجود بعد Pull"
```
الحل (للمطور): تأكد من npm run build قبل git push
```

### "فشل التحديث"
```
الحل (للعميل): 
1. تحقق من الإنترنت
2. راجع logs/update_*.log
3. اتصل بالدعم
```

---

## 📞 الدعم

**للمطور:** اقرأ `DEVELOPER_GUIDE.md`  
**للعميل:** اقرأ `CLIENT_GUIDE.md`  
**للإعداد الأولي:** اقرأ `GIT_PORTABLE_SETUP.md`

---

## ✅ Checklist

### للمطور قبل أول Push:
- [ ] Git Portable موجود في `git/`
- [ ] `frontend/dist/` موجود ومُحدث
- [ ] `.gitignore` معد بشكل صحيح
- [ ] `update.bat` تم اختباره محلياً

### للعميل عند الاستلام:
- [ ] فك ضغط المشروع
- [ ] `composer install` في Backend
- [ ] إعداد `.env`
- [ ] `php artisan migrate`
- [ ] اختبار `update.bat`

---

**🎉 الآن كل شيء جاهز!**
