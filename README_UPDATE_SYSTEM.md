# 🚀 نظام التحديث التلقائي - Car Dealership System
# Automatic Update System

## 📋 نظرة عامة

هذا المشروع مجهز بنظام تحديث احترافي يسمح للعميل بتحديث النظام **بضغطة زر واحدة** بدون الحاجة لـ:
- ❌ Node.js أو npm
- ❌ Git مثبت في النظام
- ❌ معرفة تقنية
- ❌ أوامر معقدة

---

## 🎯 المفهوم الأساسي

### المشكلة:
- العميل لا يملك Node.js
- العميل يعمل offline أغلب الوقت
- العميل يريد تحديثات سلسة

### الحل:
1. **المطور** يبني React على جهازه (`npm run build`)
2. **المطور** يرفع كل شيء بما فيه `dist` إلى GitHub
3. **العميل** يضغط على `update.bat` فقط
4. **النظام** يتولى كل شيء تلقائياً

---

## 📁 هيكل المشروع

```
car-dealer-ship/
├── 📄 update.bat                  # ملف التحديث الرئيسي
├── 📄 version.json                # معلومات الإصدار الحالي
├── 📁 git/                        # Git Portable (مضمّن)
│   ├── bin/
│   │   └── git.exe
│   └── cmd/
│       └── git.exe
├── 📁 logs/                       # سجلات التحديث
│   └── update_YYYYMMDD_HHMMSS.log
├── 📁 backups/                    # نسخ احتياطية تلقائية
│   └── db_backup_YYYYMMDD_HHMMSS.sql
├── 📁 Backend/                    # Laravel Backend
│   ├── app/
│   │   ├── Console/Commands/
│   │   │   └── DatabaseBackup.php  # أمر النسخ الاحتياطي
│   │   └── Http/Controllers/
│   │       └── UpdateSystemController.php
│   └── routes/
│       └── api.php                # Routes للتحديث
├── 📁 frontend/                   # React Frontend
│   └── dist/                      # ✅ مرفوع على GitHub
├── 📄 DEVELOPER_GUIDE.md          # دليل المطور
├── 📄 CLIENT_GUIDE.md             # دليل العميل
└── 📄 GIT_PORTABLE_SETUP.md       # دليل إعداد Git
```

---

## 🔄 كيف يعمل النظام؟

### على جهاز المطور:

```bash
# 1. التطوير
cd frontend
npm run dev

# 2. البناء
npm run build

# 3. الرفع إلى GitHub
git add .
git commit -m "Update: وصف التحديث"
git push origin main
```

### على جهاز العميل:

```
1. دبل كليك على update.bat
2. انتظر (2-5 دقائق)
3. ✅ تم!
```

---

## ✨ مميزات النظام

### 1️⃣ نسخ احتياطي تلقائي
- يتم إنشاء backup قبل كل تحديث
- محفوظ في `backups/`
- يمكن استرجاعه عند الحاجة

### 2️⃣ تسجيل كامل (Logging)
- كل تحديث له ملف log خاص
- يحتوي على تفاصيل كاملة
- مفيد للتشخيص والدعم

### 3️⃣ إدارة الإصدارات (Versioning)
- تتبع رقم الإصدار (Git commit hash)
- تاريخ آخر تحديث
- رسالة آخر commit

### 4️⃣ واجهة ويب للتحديث
- صفحة داخل Laravel للتحديث
- عرض معلومات الإصدار
- عرض السجلات والنسخ الاحتياطية
- زر لتشغيل التحديث

### 5️⃣ معالجة الأخطاء
- رسائل واضحة بالعربية والإنجليزية
- توجيه لحل المشاكل
- حماية من فقدان البيانات

---

## 🛠️ خطوات الإعداد الأولي

### للمطور (مرة واحدة):

#### 1. إعداد Git Portable

```bash
# تحميل Git Portable من:
https://git-scm.com/download/win

# فك الضغط ووضعه في:
car-dealer-ship/git/
```

راجع `GIT_PORTABLE_SETUP.md` للتفاصيل الكاملة.

#### 2. تحديث .gitignore

تأكد من أن `frontend/.gitignore` لا يمنع `dist`:

```gitignore
# ✅ الصحيح
# dist

# ❌ الخطأ
dist
```

وأضف في `.gitignore` الجذر:

```gitignore
# Git Portable
/git/

# اللوجات والنسخ الاحتياطية
/logs/
/backups/
/version.json
```

#### 3. أول Build و Push

```bash
cd frontend
npm run build
cd ..

git add .
git commit -m "Initial: نظام التحديث + dist"
git push origin main
```

#### 4. إعداد Laravel Backend

```bash
cd Backend

# إنشاء أمر Backup (تم بالفعل)
php artisan make:command DatabaseBackup

# التأكد من Routes (تم بالفعل)
# routes/api.php يحتوي على routes للتحديث
```

---

## 📖 الأدلة المتوفرة

### 1. DEVELOPER_GUIDE.md
- سير عمل المطور اليومي
- كيفية البناء والرفع
- استكشاف الأخطاء
- نصائح وأفضل الممارسات

### 2. CLIENT_GUIDE.md
- طريقة التحديث بالتفصيل
- حل المشاكل الشائعة
- شرح الملفات المهمة
- الأسئلة الشائعة

### 3. GIT_PORTABLE_SETUP.md
- تحميل وتثبيت Git Portable
- إعداد الاتصال بـ GitHub
- اختبار النظام
- إعداد الحزمة للعميل

---

## 🔌 API Endpoints

تم إضافة endpoints للتحديث في `Backend/routes/api.php`:

```php
Route::prefix('system')->group(function () {
    // عرض صفحة التحديث
    Route::get('update', [UpdateSystemController::class, 'index']);
    
    // تشغيل التحديث
    Route::post('update/run', [UpdateSystemController::class, 'runUpdate']);
    
    // إنشاء backup يدوي
    Route::post('update/backup', [UpdateSystemController::class, 'createManualBackup']);
    
    // الحصول على معلومات الإصدار
    Route::get('update/version', [UpdateSystemController::class, 'getVersionInfo']);
    
    // الحصول على السجلات
    Route::get('update/logs', [UpdateSystemController::class, 'getLogs']);
});
```

### استخدام الصفحة:

افتح المتصفح على:
```
http://localhost:8000/api/system/update
```

---

## 🔧 ملف update.bat

### ماذا يفعل؟

```batch
[1/6] النسخ الاحتياطي      → php artisan db:backup
[2/6] قراءة الإصدار         → cat version.json
[3/6] سحب التحديثات         → git pull origin main
[4/6] تحديث Composer        → composer install
[5/6] تحديث Database        → php artisan migrate --force
[6/6] تحديث الإصدار         → git rev-parse HEAD > version.json
```

### المتغيرات القابلة للتعديل:

في بداية الملف:

```batch
REM تحديد مسار Git Portable
set "GIT_EXE=%PROJECT_DIR%git\bin\git.exe"

REM تحديد branch
git pull origin main  ← غيّر main حسب الحاجة
```

---

## 🗄️ أمر النسخ الاحتياطي

تم إنشاء `DatabaseBackup.php` في:
```
Backend/app/Console/Commands/DatabaseBackup.php
```

### الاستخدام:

```bash
# نسخ احتياطي تلقائي
php artisan db:backup

# نسخ احتياطي لملف محدد
php artisan db:backup --file="../backups/manual_backup.sql"
```

### المميزات:
- يبحث تلقائياً عن `mysqldump`
- يدعم XAMPP, WAMP, MySQL
- طريقة بديلة عبر PHP إذا لم يجد mysqldump
- رسائل واضحة وتقارير مفصلة

---

## 🎨 صفحة التحديث (Laravel View)

تم إنشاء صفحة ويب جميلة في:
```
Backend/resources/views/admin/update-system.blade.php
```

### المميزات:
- تصميم عصري ومتجاوب
- عرض معلومات الإصدار
- أزرار للتحديث والـ backup
- عرض السجلات
- رسائل نجاح/فشل واضحة
- دعم اللغة العربية والإنجليزية

---

## ⚙️ متطلبات النظام

### على جهاز المطور:
- ✅ Node.js & npm
- ✅ Git
- ✅ PHP & Composer
- ✅ MySQL/MariaDB

### على جهاز العميل:
- ✅ PHP & Composer (فقط)
- ✅ MySQL/MariaDB (فقط)
- ✅ اتصال إنترنت (عند التحديث فقط)
- ❌ **لا يحتاج** Node.js
- ❌ **لا يحتاج** Git مثبت
- ❌ **لا يحتاج** معرفة تقنية

---

## 🚨 استكشاف الأخطاء

### المشكلة: "Git Portable غير موجود"

**السبب:** مجلد `git/` غير موجود أو فارغ

**الحل:**
1. تحميل Git Portable
2. وضعه في `car-dealer-ship/git/`
3. التأكد من وجود `git/bin/git.exe`

راجع `GIT_PORTABLE_SETUP.md`

---

### المشكلة: "فشل git pull"

**الأسباب المحتملة:**
- لا يوجد إنترنت
- بيانات الاعتماد خاطئة
- المشروع غير متصل بـ GitHub

**الحل:**
```powershell
# التحقق من Remote
git remote -v

# إعداد Credentials
git config --global credential.helper wincred

# إعادة المحاولة
git pull origin main
```

---

### المشكلة: "فشل composer install"

**السبب:** Composer غير مثبت أو لا يعمل

**الحل:**
```bash
# التحقق من Composer
composer --version

# إعادة تثبيت إذا لزم الأمر
# من: https://getcomposer.org/
```

---

### المشكلة: "فشل migrate"

**السبب:** خطأ في الـ migration أو قاعدة البيانات

**الحل:**
1. لا تقلق! يوجد backup في `backups/`
2. راجع ملف اللوج في `logs/`
3. استرجع الـ backup إذا لزم الأمر:
   ```bash
   mysql -u root -p database_name < backups/latest_backup.sql
   ```

---

## 📦 حزمة للعميل (Initial Deployment)

### خطوات التحضير:

```powershell
# 1. بناء Frontend
cd frontend
npm run build
cd ..

# 2. حذف ملفات غير ضرورية
Remove-Item frontend/node_modules -Recurse -Force
Remove-Item Backend/vendor -Recurse -Force

# 3. إضافة Git Portable
# ضع Git Portable في git/

# 4. ضغط المشروع
Compress-Archive -Path car-dealer-ship -DestinationPath car-dealer-ship-v1.0.zip
```

### محتويات الحزمة:

```
✅ update.bat
✅ git/ (Git Portable)
✅ frontend/dist/
✅ Backend/ (بدون vendor)
✅ CLIENT_GUIDE.md
✅ version.json
❌ node_modules
❌ Backend/vendor (سيتم تنزيله عبر composer)
```

### تعليمات العميل:

```
1. فك ضغط الملف
2. افتح Backend/
3. نسخ .env.example إلى .env
4. تعديل معلومات قاعدة البيانات في .env
5. تشغيل: composer install
6. تشغيل: php artisan key:generate
7. تشغيل: php artisan migrate --seed
8. جاهز! استخدم update.bat للتحديثات
```

---

## 🔒 الأمان

### النسخ الاحتياطية:
- ✅ تلقائية قبل كل تحديث
- ✅ محفوظة محلياً في `backups/`
- ✅ تحتوي على كل البيانات

### السجلات:
- ✅ تسجيل كامل لكل عملية
- ✅ تواريخ ووقت دقيق
- ✅ رسائل الأخطاء مفصلة

### حماية البيانات:
- ✅ لا يتم حذف أي شيء
- ✅ Rollback ممكن دائماً
- ✅ الـ .env محمي (غير مرفوع)

---

## 📞 الدعم الفني

### للمطور:
اقرأ `DEVELOPER_GUIDE.md`

### للعميل:
اقرأ `CLIENT_GUIDE.md`

### عند حدوث مشكلة:
1. ملف اللوج من `logs/`
2. محتوى `version.json`
3. لقطة شاشة للخطأ
4. وصف الخطوات التي قام بها المستخدم

---

## 🎓 أفضل الممارسات

### للمطور:

✅ **افعل:**
- بناء Frontend قبل كل Push
- اختبار البناء محلياً
- استخدام رسائل commit واضحة
- اختبار Migrations قبل الرفع

❌ **لا تفعل:**
- رفع `node_modules`
- منع `dist` في gitignore
- تعديل `update.bat` بدون سبب
- Push بدون اختبار

### للعميل:

✅ **افعل:**
- تشغيل update.bat عند وجود إنترنت
- انتظار اكتمال التحديث
- الاحتفاظ بنسخ احتياطية دورية

❌ **لا تفعل:**
- إغلاق update.bat أثناء العمل
- حذف مجلدات `git/`, `logs/`, `backups/`
- تعديل الملفات يدوياً

---

## 📊 إحصائيات

### حجم الملفات:
```
Git Portable:     ~60 MB
Backend/vendor:   ~30 MB (composer install)
frontend/dist:    ~2-5 MB
Total للعميل:    ~100 MB
```

### وقت التحديث:
```
[1/6] Backup:     5-10 ثواني
[2/6] Version:    1 ثانية
[3/6] Git Pull:   10-30 ثانية (حسب الإنترنت)
[4/6] Composer:   30-60 ثانية
[5/6] Migrate:    5-10 ثواني
[6/6] Version:    1 ثانية

Total: 2-5 دقائق
```

---

## 🌟 ملخص سريع

### المطور:
```bash
npm run build && git add . && git commit -m "Update" && git push
```

### العميل:
```
دبل كليك على update.bat
```

**بهذه البساطة! 🎉**

---

## 📅 تاريخ الإصدارات

| الإصدار | التاريخ | الوصف |
|---------|---------|-------|
| 1.0.0 | 2025-01-22 | نظام التحديث التلقائي - Initial Release |

---

## 📝 TODO (اختياري للتحسين المستقبلي)

- [ ] إضافة إشعار تلقائي عند توفر تحديث
- [ ] ضغط النسخ الاحتياطية القديمة
- [ ] حذف تلقائي للوجات القديمة (أكبر من 30 يوم)
- [ ] إضافة Rollback بضغطة زر
- [ ] دعم تحديثات متعددة اللغات
- [ ] إحصائيات الاستخدام والأداء

---

## 🎉 شكراً

نظام مصمم بعناية لتوفير تجربة تحديث سلسة للعميل وسهولة صيانة للمطور.

**Happy Coding! 🚀**

---

## 📄 الملفات المُنشأة

تم إنشاء الملفات التالية تلقائياً:

- ✅ `update.bat` - ملف التحديث الرئيسي
- ✅ `version.json` - معلومات الإصدار
- ✅ `DEVELOPER_GUIDE.md` - دليل المطور
- ✅ `CLIENT_GUIDE.md` - دليل العميل
- ✅ `GIT_PORTABLE_SETUP.md` - دليل إعداد Git
- ✅ `Backend/app/Console/Commands/DatabaseBackup.php` - أمر Backup
- ✅ `Backend/app/Http/Controllers/UpdateSystemController.php` - Controller
- ✅ `Backend/resources/views/admin/update-system.blade.php` - صفحة الويب
- ✅ `frontend/.gitignore` - معدّل (يسمح برفع dist)

---

**الآن النظام جاهز للاستخدام! 🎊**
