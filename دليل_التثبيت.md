# 🚗 دليل تثبيت نظام معرض السيارات
# Car Dealership System - Installation Guide

---

## ⚠️ المتطلبات الأساسية

يجب أن تكون مثبتة على جهازك:

- ✅ **XAMPP** (يحتوي على PHP + MySQL)
  - تحميل من: https://www.apachefriends.org/
  
- ✅ **Composer** (لإدارة مكتبات PHP)
  - تحميل من: https://getcomposer.org/

### ❌ لا تحتاج:
- Node.js
- npm  
- Git

---

## 📦 خطوات التثبيت

### الخطوة 1: فك ضغط الملف

1. استخرج محتويات الملف المضغوط إلى المكان المطلوب
2. مثال: `C:\xampp\htdocs\car-dealer-ship\`

---

### الخطوة 2: تشغيل XAMPP

1. افتح **XAMPP Control Panel**
2. شغّل **Apache**
3. شغّل **MySQL**

---

### الخطوة 3: تشغيل ملف التثبيت

1. افتح مجلد المشروع
2. ابحث عن ملف: **`INSTALL.bat`**
3. اضغط عليه **دبل كليك**
4. اتبع التعليمات على الشاشة
5. انتظر حتى اكتمال التثبيت (2-3 دقائق)

**سيقوم الملف تلقائياً بـ:**
- ✅ فحص وجود PHP و Composer
- ✅ تثبيت مكتبات PHP
- ✅ إنشاء ملف الإعدادات
- ✅ إنشاء المجلدات المطلوبة

---

### الخطوة 4: إنشاء قاعدة البيانات

1. افتح المتصفح واذهب إلى: **http://localhost/phpmyadmin**

2. اضغط على **"New"** أو **"جديد"** في القائمة اليسرى

3. أدخل اسم قاعدة البيانات، مثلاً: `car_dealership`

4. اختر **utf8mb4_unicode_ci** من القائمة

5. اضغط **"Create"** أو **"إنشاء"**

✅ تم إنشاء قاعدة البيانات!

---

### الخطوة 5: إعداد الاتصال بقاعدة البيانات

1. افتح مجلد: **`Backend\`**

2. ابحث عن ملف: **`.env`**

3. افتحه بأي محرر نصوص (Notepad, Notepad++, VSCode)

4. ابحث عن هذه الأسطر وعدّلها:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=car_dealership        ← ضع اسم القاعدة
DB_USERNAME=root                  ← المستخدم (عادة root)
DB_PASSWORD=                      ← كلمة المرور (فارغة في XAMPP)
```

5. **احفظ الملف** (Ctrl+S)

---

### الخطوة 6: إنشاء جداول قاعدة البيانات

1. افتح **PowerShell** أو **Command Prompt**

2. اذهب لمجلد Backend:
```bash
cd C:\xampp\htdocs\car-dealer-ship\Backend
```

3. شغّل الأمر:
```bash
php artisan migrate
```

4. إذا سألك `Do you want to run this migration?`
   اكتب: **`yes`** واضغط Enter

5. انتظر حتى ترى: `Migration completed successfully`

✅ تم إنشاء الجداول!

---

### الخطوة 7 (اختياري): إضافة بيانات تجريبية

إذا أردت بيانات تجريبية للاختبار:

```bash
php artisan db:seed
```

---

### الخطوة 8: تشغيل النظام

في نفس نافذة الأوامر (داخل مجلد Backend):

```bash
php artisan serve
```

ستظهر رسالة:
```
INFO  Server running on [http://127.0.0.1:8000]
```

**⚠️ مهم:** لا تغلق هذه النافذة! اتركها مفتوحة أثناء استخدام النظام.

---

### الخطوة 9: فتح النظام

1. افتح المتصفح (Chrome, Firefox, Edge)

2. اذهب إلى:
```
http://localhost:8000
```

أو

```
http://127.0.0.1:8000
```

**🎉 مبروك! النظام يعمل الآن!**

---

## 🔄 التحديثات المستقبلية

عندما يتوفر تحديث جديد:

### الطريقة السهلة:

1. تأكد من اتصالك بالإنترنت
2. اضغط دبل كليك على: **`update.bat`**
3. انتظر 2-5 دقائق
4. ✅ تم التحديث تلقائياً!

**النظام سيقوم بـ:**
- ✅ نسخ احتياطي لقاعدة البيانات
- ✅ تحميل آخر التحديثات
- ✅ تحديث المكتبات
- ✅ تحديث قاعدة البيانات

---

## 🆘 حل المشاكل الشائعة

### ❌ المشكلة: "composer: command not found"

**السبب:** Composer غير مثبت

**الحل:**
1. حمّل Composer من: https://getcomposer.org/
2. ثبّته واختر PHP من XAMPP
3. أعد تشغيل INSTALL.bat

---

### ❌ المشكلة: "Could not find driver"

**السبب:** PHP extensions غير مفعّلة

**الحل:**
1. افتح ملف: `C:\xampp\php\php.ini`
2. ابحث عن هذه الأسطر وأزل `;` من بدايتها:
```ini
;extension=pdo_mysql     ← احذف ;
;extension=mysqli        ← احذف ;
```
3. احفظ الملف
4. أعد تشغيل Apache من XAMPP

---

### ❌ المشكلة: "Access denied for user 'root'@'localhost'"

**السبب:** بيانات قاعدة البيانات خاطئة في `.env`

**الحل:**
1. افتح `Backend\.env`
2. تحقق من:
   - `DB_DATABASE` = اسم قاعدة البيانات الصحيح
   - `DB_USERNAME` = عادة `root`
   - `DB_PASSWORD` = فارغ في XAMPP (احذف أي نص بعد `=`)
3. احفظ
4. في Command Prompt:
```bash
cd Backend
php artisan config:clear
php artisan migrate
```

---

### ❌ المشكلة: صفحة بيضاء أو خطأ 500

**الحل:**
```bash
cd Backend
php artisan cache:clear
php artisan config:clear
php artisan route:clear
```

ثم أعد فتح المتصفح.

---

### ❌ المشكلة: "Git Portable غير موجود" عند التحديث

**السبب:** مجلد `git` غير موجود أو فارغ

**الحل:** لا تقلق! النظام يعمل بدون Git. فقط لن تستطيع استخدام update.bat. اتصل بالدعم الفني للحصول على نسخة محدثة.

---

## 📋 ملخص الأوامر

```bash
# الانتقال لمجلد Backend
cd C:\xampp\htdocs\car-dealer-ship\Backend

# إنشاء جداول قاعدة البيانات (مرة واحدة)
php artisan migrate

# بيانات تجريبية (اختياري)
php artisan db:seed

# تشغيل الخادم (في كل مرة)
php artisan serve

# مسح الذاكرة المؤقتة (عند المشاكل)
php artisan cache:clear
php artisan config:clear
```

---

## ✅ Checklist التثبيت

- [ ] تثبيت XAMPP
- [ ] تثبيت Composer
- [ ] تشغيل Apache + MySQL من XAMPP
- [ ] فك ضغط المشروع
- [ ] تشغيل `INSTALL.bat`
- [ ] إنشاء قاعدة بيانات في phpMyAdmin
- [ ] تعديل `Backend\.env`
- [ ] تشغيل `php artisan migrate`
- [ ] تشغيل `php artisan serve`
- [ ] فتح المتصفح على `http://localhost:8000`
- [ ] ✅ النظام يعمل!

---

## 🚀 الاستخدام اليومي

### لتشغيل النظام:

1. تشغيل XAMPP (Apache + MySQL)
2. فتح PowerShell/Command Prompt
3. تنفيذ:
```bash
cd C:\xampp\htdocs\car-dealer-ship\Backend
php artisan serve
```
4. فتح المتصفح: `http://localhost:8000`

### لإيقاف النظام:

- اضغط `Ctrl+C` في نافذة الأوامر
- أغلق XAMPP

---

## 🎯 صفحة التحديث من الويب

يمكنك أيضاً التحديث من المتصفح:

1. افتح: `http://localhost:8000/api/system/update`
2. اضغط زر **"تحديث النظام"**
3. انتظر حتى تظهر رسالة النجاح

---

## 💾 النسخ الاحتياطية

- ✅ يتم إنشاء نسخة احتياطية **تلقائياً** قبل كل تحديث
- ✅ النسخ محفوظة في مجلد: `backups\`
- ✅ اسم الملف يحتوي على التاريخ والوقت
- ✅ يمكن استرجاعها من phpMyAdmin

---

## 📊 معلومات المشروع

**الاسم:** نظام إدارة معرض السيارات  
**الإصدار:** 1.0.0  
**التاريخ:** نوفمبر 2025

**Backend:** Laravel 11  
**Frontend:** React 18 + TypeScript  
**Database:** MySQL/MariaDB

---

## 📞 للدعم الفني

إذا واجهت أي مشكلة:

1. راجع قسم "حل المشاكل الشائعة" أعلاه
2. تحقق من ملفات اللوج في: `logs\`
3. اتصل بالدعم الفني وأرسل:
   - لقطة شاشة للخطأ
   - محتوى `logs\update_*.log` (إن وُجد)
   - محتوى `version.json`

---

## 🎉 تهانينا!

أنت الآن جاهز لاستخدام النظام!

**للتحديثات المستقبلية:** فقط اضغط على `update.bat` 

**بهذه البساطة! 🚀**

---

_آخر تحديث: نوفمبر 2025_
